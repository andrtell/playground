# import dsa.binary_tree.node.iter as iter
#
#
# def horizontal_line(node, depth):
#     pass


# def horizontal(node):
#     buff = []
#     tree = list(iter.reverse_in_order(node))
#     for n, d in range(0, len(tree)):
#         n, d = tree[i]
#         line = d * "  |" + str(n.value)
#         buff.append(line)
#     return "\n".join(buff)
